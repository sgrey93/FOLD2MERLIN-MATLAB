To use FOLD2MERLIN in MERLIN2 just use the lines in MERLIN2:

filename = 'XXX.FOLD';
[Node,Panel] = FOLD2MERLIN(filename);

Where XXX is your filename of the .FOLD file saved in the MERLIN2 directory.
